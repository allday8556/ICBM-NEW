"""SmartStore CONNECT (M2 PR-A): credentials, the committed session, the account binding and the
evidence the capability owner converges on. Provider calls go through the registry-gated caller
in ``integrations.marketplaces.smartstore`` only."""
