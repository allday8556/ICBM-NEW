# M4 — canonical Product DB: acceptance plan and offline harness

- Status: **ACCEPTED** 2026-09-19, **bounded by §2**. The acceptance covers exactly the claim of §1. It was proven by the final offline acceptance run of the merged harness (§7): exact merged main `57a66760bc1e48c24633e952352f218949a9d51e`, a clean checkout and a new dedicated root, **146/146 checks**, `problems: []`. Architect acceptance: Issue #80 comment 5740024056. Run instruction: 5739991193. Run evidence: 5740012837. The PR-head rehearsals were never the acceptance.
- Milestone: M4 canonical Product DB (Issue #80)
- Owning contracts: ADR-0013 (clarified by ruling 5738760913), ADR-0010, the quantity-priced rule 5737762202; PR-A #81, PR-B #82, PR-C #83, PR-D #84, PR-E #85, PR-Q #86, PR-F #87
- Accepted code: `57a66760bc1e48c24633e952352f218949a9d51e` (main after PR #87), Alembic head `0015_m4_quantity_offers`
- Final report digest: `daab3f4512a696ce25b79bb92f18deaf7b8114deb41abfe6ac5fbb4d5b2c7bed`
- Out of scope by decision: quantity-scoped resale/MAP guidance (5738886070) is an M5 registration-screen aid, not M4 pricing truth
- Runtime implementation: Claude Code
- Architecture/audit: ChatGPT + independent cross-audit

---

## 1. What M4 claims

Accepted COLLECT source truth traverses the M4 production owners end to end, on a fresh dedicated data root:

```text
ProductFactsRevision / SourceAsset
→ current source revision
→ ProductGroup (the canonical Product) and its membership
→ ListingComposition / Item
→ BASE_PRODUCT or SOURCE_OFFER binding
→ PricingSnapshot per Item and pricing context
→ derived image lineage, operator image selection, exact-binary QA
→ base and pricing readiness (derived, never stored)
→ durable read-back
→ process restart
→ source revision change
→ correct staleness and explicit refresh
```

## 2. Bounded scope

M4 does **not** claim:
- **Source SKUs.** Positive option-axis or configuration SourceSKU materialization, and SKU-specific tier mapping. Only product-level quantity offers exist (ruling 5738760913).
- **Composed fulfillment.**
- **Grouping.** Cross-supplier grouping, MERGE or SPLIT. Consequently no M4 rule produces `DUPLICATE`, and the harness reports it as `NOT_EXERCISED`. Its place in the precedence is pinned by a unit test.
- **M5 work.**
  - Any marketplace listing, option, category, notice or preflight.
  - Any registration candidate. `RegisterService.registration_candidate_count()` stays 0.
- **Provider work.** Any supplier, marketplace, AI or OCR call. The harness has no such capability.
- **Quantity resale guidance.** Quantity-scoped resale-price guidance (5738886070).

## 3. The harness

```text
python scripts/m4_acceptance.py --root <fresh-dedicated-root>
python scripts/m4_acceptance.py --verify <root>/m4-acceptance-report.json
```

**Exit status:**
- 0: the report has no problem;
- 1: a check failed, or a report does not verify;
- 2: the root or the checkout was refused, before anything was created.

No approval phrase exists, because the run has no provider capability.

### 3.1 The root (`scripts/m4accept/root.py`)

A root is accepted only when every one of these holds:
- **No preserved campaign.** No path component names a preserved campaign runtime (`m3-recon-*`, `m3-accept-*`, in any case or separator). This is judged on the path as given, before anything is created or listed, and again once resolved.
- **Dedicated.** The root lies outside the repository and outside the default and configured ICBM data directories.
- **Fresh.** It is new, empty, or holds only the harness marker in state `INITIALIZED`.

A root that ran once is never reused. Only the root itself is ever listed; no sibling directory is enumerated. Application data lives under `<root>/data`. The marker `m4-acceptance-root.json` ends `PASSED` or `FAILED`.

### 3.2 The checkout (`scripts/m4accept/checkout.py`)

A run is evidence for a commit only when the code it executes is exactly that commit. After the root is judged and before it is claimed, the run is **refused** (exit 2, nothing created) unless every one of these holds:
- **A commit.** The code's own repository top level answers `git rev-parse HEAD`.
- **No tracked change.** `git status` shows no staged or unstaged change, and no tracked file is hidden from it (assume-unchanged or skip-worktree).
- **No untracked file.** Nothing untracked and not ignored is in the working tree.
- **No ignored source.** No ignored Python source, sourceless bytecode, path file or extension module is in the working tree outside the running interpreter's own environment. Cached bytecode beside its source and ignored data are not code the run can execute.
- **Loaded from the checkout.** Every loaded `app`, `integrations` and `scripts` module comes from this checkout.

A count that cannot be measured is unknown, and unknown refuses the run too. The checkout is measured again when the run ends; any change fails the check `checkout.unchanged_during_run`. The report keeps the exact HEAD and the measured counts. The root lies outside the repository, so the run never dirties the tree itself.

### 3.3 Offline guards (`scripts/m4accept/guards.py`)

Each guard is armed for the run only, and every count is measured:
- **Network.** The process egress audit hook counts every blocked non-loopback attempt. No supplier egress grant may open.
- **Imports.** A meta-path guard refuses AI, OCR, marketplace, SmartStore and supplier CONNECT services, supplier transports, the KM collection, browser automation, HTTP clients and the collection job. Each attempt is counted, and the kinds of module loaded during the run are listed. A module of that kind **already loaded when the guard arms** (the harness's own import graph loads before the run starts) is listed as preloaded and fails the hard zero (`hard_zero.no_forbidden_module_preloaded`); it is never subtracted as a baseline. ORM table modules are not provider paths; the schema aggregate loads them, so they are the one explicit exception, and they are not on the forbidden list.
- **Paths.** A file-open, listing and database audit hook refuses and counts any path naming a preserved campaign. It counts every write outside the root, bytecode caches aside. A SQLite connection counts as a write unless its URI opens it `mode=ro`; only in-memory databases have no file.

The four provider counters (supplier writes, marketplace reads/writes, AI calls, OCR calls) are reported as 0 only when every guard held. Otherwise they are reported as unknown, never as an invented 0.

### 3.4 Composition (`scripts/m4accept/owners.py`)

The run composes exactly the production classes `app.container` wires, over the same database, clock, audit log and data-root directories:
- the M4 owners: materializer, products, pricing, images and readiness;
- COLLECT's run, revision and source-asset stores.

No CONNECT service, supplier gateway, collection transport, SmartStore caller, job worker or secret store exists in the run. The data root is owned as the application owns it (ADR-0006): the lease comes first, and it is released only after the database is disposed.

### 3.5 Synthetic source truth (`scripts/m4accept/synthetic.py`)

Every input is invented: the supplier key `m4-synthetic`, the product identities, names, amounts and PNG bytes. The source truth is written only through COLLECT's own stores:
- the source-asset store keeps the bytes;
- the collection-run store opens a run and settles it RECORDED;
- the revision store validates and appends the revision.

No raw row is inserted, and no M4 truth is written by SQL. The only SQL is read-only (`mode=ro`) evidence.

## 4. Scenarios

The pricing context is invented: `m4_market`, no account, fee rate 10%, other costs 0, `CEIL_KRW_1`, policy v1 (35% target, 10% minimum).

**S1 BASE_PRODUCT.**

| step | expected |
|---|---|
| R1: price 10,000, FIXED shipping 2,500, minimum 18,000, options/tiers ABSENT, ON_SALE, two CONFIRMED images | one ProductGroup, one CONFIRMED member (membership revision 1), the default single-unit Item, a BASE_PRODUCT binding on R1 |
| pricing R1 | target 22,728 is **above** the minimum. Final **18,000 = the minimum**, basis `MINIMUM_SALE_PRICE`, fee 1,800, profit 3,700, margin 2,055 bp, guard OK |
| images R1 | a completed derivation of the representative image, stored apart from source assets. An operator selection (derived representative + source detail): readiness first names the missing selection, then the missing QA. PASS QA for both exact binaries: base and pricing READY |
| restart | every identity, pointer and readiness fingerprint reads back identically, with no row or byte written. Materialization and pricing replays are UNCHANGED |
| R2: price 11,000 | same source product, group and Item; R1 unchanged; the binding closed and replaced; the old snapshot kept. Pricing STALE (`PRICING_SNAPSHOT_SUPERSEDED`); images STALE (`IMAGE_SELECTION_STALE`, `IMAGE_DERIVATION_STALE`) |
| refresh R2 | repriced: final 18,000, profit 2,700. A new derivation of the same artifact; RESELECTED; QA under R2. READY again, and no history rewritten |

**S2 QuantityOffer.** Generic prices are 11,111 and 33,333, which equal no tier total, multiple or quotient.

| step | expected |
|---|---|
| Q1: tiers 1 = 19,900, 2 = 37,900, 3 = 53,900; FREE shipping; no minimum | three exact offers, no SourceSKU; three Items (q1 = the default signature); exact SOURCE_OFFER bindings with fulfillment 1/2/3 |
| pricing Q1 | costs 19,900 / 37,900 / 53,900 → finals 36,184 / 68,910 / 98,000, all TARGET_MARGIN/OK |
| images Q1 | every Item starts REVIEW_REQUIRED (`IMAGE_SELECTION_MISSING`). q1's selection is not inherited by q2/q3. One selection per Item, QA per exact binary: READY |
| Q2: tiers 2 = 36,900, 3 = 52,900 | same Items; three new revision-scoped offers, Q1's kept; bindings replaced. Pricing and images STALE, then repriced (36,184 / 67,093 / 96,184), reselected and READY |

**S3 BLOCKED.**

| step | expected |
|---|---|
| SOLD_OUT, price 10,000, FIXED 2,500, minimum 11,000 | base BLOCKED (`SOURCE_STOCK_SOLD_OUT`). Final 11,000 = the minimum, guard LOSS with both guard reasons: pricing BLOCKED |

**Final restart.** The full read-back of all three products and five Items matches durable state. No row or byte is written, and every replay is UNCHANGED. Every row and stored byte that S1 made durable before its first restart is still identical; only an open binding may have closed its window.

## 5. The report

`<root>/m4-acceptance-report.json` (schema `icbm-m4-acceptance-report/v1`) holds:
- the run id, the exact code SHA, the measured checkout counts and the alembic head;
- the scenario identities and exact amounts;
- the restart proofs;
- the readiness examples (`READY`, `REVIEW_REQUIRED`, `STALE`, `BLOCKED`; `DUPLICATE` `NOT_EXERCISED`);
- the hard-zero evidence and the preserved-campaign access count;
- every check with its result;
- `problems[]`;
- `report_digest`, SHA-256 over the canonical JSON of everything else.

**Sanitizing.** Before the report is written, it is scanned for:
- URLs and absolute or backslash paths;
- the run's own paths;
- secret-like words and the approval-phrase keyword;
- non-ASCII business text.

A leaking report is withheld and replaced by its problems.

## 6. Evidence and closeout

- **PR-head rehearsals.** Each showed only that the harness and the implementation agree, and neither was the acceptance:
  - `m4-rehearsal-01` on `5c90f66`: 144/144 checks (Issue #80 comment 5739726205);
  - `m4-rehearsal-02` on `bcf6e61`, after GPT review 5254796929: 146/146 checks (5739916420).
- **Gates before the final run.** PR-F (#87) passed the GPT exact-head re-audit (review 5254872072) and the Claude AI cross-audit (5739934200). The user authorized the merge (5739939448), and it merged as `57a66760` (5739991048).
- **Final acceptance.** The harness ran once more, as instructed in 5739991193, from a clean checkout of the exact merged main and on a new root. §7 records the report (evidence 5740012837). The architect accepted it in 5740024056, and M4 is **ACCEPTED** on that record.
- **Status transition.** Under the active-milestone invariant (Issue #52 §0, PR #55 review 5204359614), the canonical CURRENT pointer, and with it `app.MILESTONE`, advance to **M5**, as M3's closeout did (ruling 5725017620). This is a status transition only. No M5 scope, contract or implementation is authorized by it.

## 7. Final acceptance record (exact merged main)

**Run.** `python scripts/m4_acceptance.py --root <new root>`, then `--verify <new root>/m4-acceptance-report.json`.
- The merged harness ran unchanged, from a clean checkout of exact main `57a66760bc1e48c24633e952352f218949a9d51e` (`HEAD` = `origin/main`).
- The root was new, named `m4-final-01`. It lay outside the repository and the default and configured ICBM data directories, and it reused neither rehearsal root.
- The run exited 0 with an empty stderr. `--verify` reported the digest verified.
- The whole report was inspected, not only its digest: **0 failed checks** and no leak.

| field | value |
|---|---|
| `code_sha` | `57a66760bc1e48c24633e952352f218949a9d51e` |
| clean-checkout counts, at start and again at the end (`checkout.unchanged_during_run`) | tracked changes 0, hidden tracked files 0, untracked files 0, ignored sources 0, code outside the checkout 0 |
| Alembic head / database revision | `0015_m4_quantity_offers` / `0015_m4_quantity_offers` |
| root marker | `PASSED`, with the report's run id |
| checks | **146 / 146** |
| `problems` | **`[]`** |
| `report_digest` | **`daab3f4512a696ce25b79bb92f18deaf7b8114deb41abfe6ac5fbb4d5b2c7bed`** |

**Results** (pricing context `m4_market`, fee 0.1, other costs 0, `CEIL_KRW_1`):

| proof | result |
|---|---|
| BASE_PRODUCT canonical minimum (S1) | R1: target 22,728 > minimum 18,000, so final **18,000**, basis `MINIMUM_SALE_PRICE`, fee 1,800, profit 3,700, 2,055 bp, OK. R2: final 18,000 (profit 2,700, 1,500 bp). No `max(target, minimum)`. The same source product, group and default Item; the R1 binding closed and replaced |
| QuantityOffer exact totals (S2) | Q1 offers (1, 19,900), (2, 37,900), (3, 53,900) → finals **36,184 / 68,910 / 98,000**. Q2 offers (1, 19,900), (2, 36,900), (3, 52,900) → finals **36,184 / 67,093 / 96,184**. There is no SourceSKU, no unit price or multiple, and the generic prices 11,111 / 33,333 are unused. There are three distinct Items (q1 = the default signature) with exact SOURCE_OFFER bindings (fulfillment 1/2/3); the same Items across Q2, Q1's offers kept |
| restart and read-back | after S1: 1 product / 1 Item; final: 3 products / 5 Items. Both read back identically with the database digest unchanged. Materialization and pricing replays are UNCHANGED and wrote nothing |
| source and history immutability | R1 is field-for-field unchanged after R2. History since S1 shows `changes: []`: no revision, snapshot, offer, derivation, selection or QA rewritten, and only open bindings closed their windows. Stored source and derived bytes are unchanged and content-addressed (5 source assets, 1 derived artifact) |
| image lineage, selection and QA | the derived artifact is stored apart from SourceAssets. The operator selection is required first, then exact-binary QA. After R2 the old selection and derivation are STALE; a new derivation of the same artifact was RESELECTED and QA'd under R2. q1's selection is not inherited by q2/q3; each Item owns its own selection |
| readiness states | READY (S1 after R1 and after the R2 refresh; S2 after selection and QA); REVIEW_REQUIRED (S2 q1–q3 before their own selection); STALE (S1 after R2; S2 after Q2); BLOCKED (S3: `SOURCE_STOCK_SOLD_OUT`; LOSS with `PRICE_LOSS`, `PRICE_BELOW_MIN_MARGIN`); DUPLICATE `NOT_EXERCISED` (§2) |
| REGISTER boundary | registration candidates **0**, no registration or draft table |
| supplier/marketplace effects | supplier/marketplace audit events **0**, jobs **0** |

**External hard zero, measured by the run's guards:**
- external network attempts **0**;
- egress grants **0**;
- forbidden modules preloaded **`[]`**, imports blocked **`[]`**, loaded during the run **`[]`**;
- supplier writes **0**;
- marketplace reads/writes **0**;
- AI calls **0**;
- OCR calls **0**;
- writes outside the root **0**, with SQLite counted unless opened `mode=ro`;
- preserved campaign access **0**.

The four provider counters are 0 only because every guard held; otherwise the harness would report `null`. No preserved M3 campaign runtime was read, migrated, copied, enumerated or reused.

**Repository unmodified.** The repository was measured just before and just after the run:
- `HEAD`, `git status` (0 entries) and assume-unchanged/skip-worktree flags (0) were unchanged;
- a SHA-256 over the content of all 447 tracked files was identical;
- every ignored file other than bytecode was unchanged.

Only 8 ignored interpreter caches, `scripts/m4accept/__pycache__/*.pyc`, were regenerated.

## 8. Decisions and gates (Issue #80)

| reference | what |
|---|---|
| ADR-0013, rulings 5737762202 and 5738760913 | the M4 contract, the quantity-priced rule, product-level quantity offers |
| 5738886070 | quantity resale/MAP guidance stays out of M4 (M5 registration aid) |
| 5739459941 | PR-F kickoff: the offline acceptance harness |
| review 5254796929 → 5254872072 | PR-F HOLD (exact checkout, preloaded providers, writable SQLite) → re-audit PASS |
| 5739934200, 5739939448, 5739991048 | Claude AI cross-audit PASS; merge authorization; PR #87 merged as `57a66760` |
| 5739991193 → 5740012837 → **5740024056** | final run instruction → final run evidence → **architect final acceptance PASS** |
