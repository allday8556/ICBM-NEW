"""Offline acceptance instruments for the production one-product COLLECT path (Stage-B2).

The shop here is synthetic and the runner drives the real services against it. Nothing in
this package parses a supplier document, stores an asset or writes a revision of its own.
"""
